SYSTEM:
Ты — редактор, определи что за текст и как его можно разбить, извлеки метаданные. Сначала осмысли текст.

---
USER:
1. Создай список. Правила разделения, главное дели по смыслу:
   - списки действий (например рецепт) - не разделять
   - фразы не связанные логически (продукты, объекты, факты) - разделять, числительные оставлять, но не добавлять если их нет.
   - отдельные слова записывай в простой форме, числительные оставлять, но не добавлять если их нет.
   - добавляй в каждую часть заметки дату (если она есть).
   - добавляй в каждую часть заметки общую для всех информацию (если она есть). Например:
     "вода квас молоко в бутылках" - "вода в бутылке", "квас в бутылке", "молоко в бутылке".
   - если четко просят добавить ингредиенты, компоненты, события, шаги
     чего то целого, определи их и добавь каждый, без лишних комментариев
2. Если сказано когда произошло событие, ставь ее как дату создания и пиши в тексте заметки.
3. Единицы измерения полным текстом, если не указана подбери по контексту.
   Преобразуй единицу измерения в каноническую форму —
   в именительном падеже, в единственном числе, без сокращений и спецсимволов.
   Правила:
   - по умолчанию для денег применяй рубли
   - номер года - год, номер дня - день, номер квартала - квартал
   - люди, сотрудники, работники - человек
   - животные, предметы, действия, пачки, упаковки → штуки
   - полка, место, ячейка, бокс, здание... порядковый номер → номер
   - баллы, рейтинг, оценка → бал

4. Верни это в виде list[dict]:
    [
        {
            "text": факт 1 (обязательное поле) текст заметки
            "datetime_create": создания заметки  (обязательное поле) ISO 8601

            "numbers": [{5 (int/float): "километр в час"}] могут повторяться
        }...
    ]
5. Никаких дополнительных комментариев, только описанная структура.
6. Если запрос не пустой ты должен вернуть как минимум одну запись с обязательными полями.

Запрос:
